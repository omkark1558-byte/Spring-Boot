package com.project.office_Employee.enitiy;


@Service
public class EmployeeService {

    @Autowired
    private EmployeeRepository repo;

    public List<Employee> getAllEmployees() {
        return repo.findAll();
    }

    public void save(Employee emp) {
        repo.save(emp);
    }

    public void delete(Long id) {
        repo.deleteById(id);
    }
}